import { StyleSheet } from 'react-native';

export const typography: any = StyleSheet.create({
  caption: {
    fontStyle: 'normal',
    fontWeight: 'normal',
    fontSize: 14,
    lineHeight: 15,
  },
});
